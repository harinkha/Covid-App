class Task {
  String title;
  String endTime;
  String startTime;
  String note;
  String date;
  bool isCompleted;
  String repeat;

  Task(this.title, this.date, this.endTime, this.isCompleted, this.note,
      this.repeat, this.startTime);
}
